info:
  name: get-players
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/draw/:drawId/players
  params:
    - name: drawId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
