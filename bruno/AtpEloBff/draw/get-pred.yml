info:
  name: get-pred
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/draw/:drawId/prediction
  params:
    - name: drawId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
